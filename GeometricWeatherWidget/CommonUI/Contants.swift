//
//  Contants.swift
//  GeometricWeatherWidgetExtension
//
//  Created by 王大爷 on 2021/10/11.
//

import Foundation
import SwiftUI
import GeometricWeatherTheme
import GeometricWeatherSettings

let largeTemperatureFont = Font.system(
    size: 48.0,
    weight: .regular
)

let largeWeatherIconSize = 42.0
let normalWeatherIconSize = 24.0
let miniWeatherIconSize = 20.0

let aqiWindBackground = Color.white.opacity(0.2)
let aqiWindCornerRadius = 4.0

let histogramBackground = Color.white.opacity(0.33)
let histogramForeground = Color.white.opacity(0.66)
let histogramShadowColor = Color.white.opacity(0.66)
let horizontalHistogramShadowRadius = 4.0
let horizontalHistogramShadowOffsetX = 0.0
let horizontalHistogramShadowOffsetY = 2.0
let verticalHistogramShadowRadius = 4.0
let verticalHistogramShadowOffsetX = 0.0
let verticalHistogramShadowOffsetY = 2.0

let secondaryTextOpacity = 0.7

let progressBackground = Color.white.opacity(0.5)
